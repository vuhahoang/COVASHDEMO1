<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covash_demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COVASHDEMO">
        <activity android:name=".ctdetail"></activity>
        <activity android:name=".NewDetail" />
        <activity android:name=".ShareUs" />
        <activity android:name=".CountryDashboard" />
        <activity android:name=".CountrySwitchData" />
        <activity android:name=".homepage" />
        <activity android:name=".registerform" />
        <activity android:name=".loginform" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>